tosca_definitions_version: tosca_simple_yaml_1_2

metadata:
  id: "microservice_12312124"
  name: "Object Detection for faulty parts"
  author: "DFKI"
  date: 06/04/2021
  version: "1.0"
  classificationSchema: "other"
  type:
    - "neural network"
    - "deep learning"

description: "This microservices solves a certain problem using very specific methods. It supports some special input types, and allows results to be presented in chosen format."

imports:
  - https://raw.githubusercontent.com/micado-scale/tosca/digitbrain/micado_types.yaml

repositories:
  docker_hub: https://hub.docker.com/
  dbs_registry: https://dbs-container-repo.emgora.eu/v2/

node_types:
  dbs.microservice.microservice_12312124:
    derived_from: dbs.microservice
    properties:
      n1:
        type: "int"
        description: "sample n1"
        default: "45"
        required: True
      n2:
        type: "bool"
        description: "sample n2"
        default: "45"
        required: False

topology_template:
  inputs:
    n1:
      type: "int"
      description: "sample n1"
      default: "45"
      required: True
    n2:
      type: "bool"
      description: "sample n2"
      default: "45"
      required: False

  substitution_mappings:
    node_type: "dbs.microservice.microservice_12312124"

  node_templates:
    busybox-sleep-pod:
      type: tosca.nodes.MiCADO.Kubernetes
      interfaces:
        Kubernetes:
          create:
            inputs:
              apiVersion: v1
              kind: Pod
              metadata:
                name: busybox-sleep
              spec:
                containers:
                - name: busybox
                  image: busybox
                  args:
                  - sleep
                  - '1000000'

